{
	"name": "altruis",
	"version": "1.0.0",
	"description": "This is a discord bot which I made for my personal uses. The main purpose of using it is to reduce my dependency on multiple bots to manage a single server.",
	"main": "index.js",
	"scripts": {
		"build": "tsc",
		"deploy": "ts-node-dev -r tsconfig-paths/register --transpile-only src/deploy.ts",
		"dev": "ts-node-dev -r tsconfig-paths/register --respawn --transpile-only src/index.ts",
		"test": "echo \"Error: no test specified\" && exit 1"
	},
	"keywords": [],
	"author": "",
	"license": "ISC",
	"dependencies": {
		"@discordjs/voice": "^0.16.1",
		"discord.js": "^14.14.1",
		"dotenv": "^16.4.5"
	},
	"devDependencies": {
		"@eslint/js": "^9.1.1",
		"eslint": "^8.57.0",
		"globals": "^15.0.0",
		"ts-node-dev": "^2.0.0",
		"tsconfig-paths": "^4.2.0",
		"typescript": "^5.4.5",
		"typescript-eslint": "^7.7.1"
	}
}
